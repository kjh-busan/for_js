// 요소 선택 및 상수 선언
const numbersDiv = document.getElementsByClassName('numbers');
numbersDiv.textContent = "kjh";

console.log(numbersDiv.textContent, 'asdfdf');
const colors = ['orange', 'skyblue', 'red', 'purple', 'green']

// paintNumber 함수
function paintNumber(number){
}

// 추첨 버튼 클릭 이벤트 핸들링
drawButton.addEventListener('click', function(){
})

// 다시 버튼 클릭 이벤트 핸들링
resetButton.addEventListener('click', function(){
})

// pop 함수를 사용하여 배열의 마지막 요소를 삭제
// shift 함수를 사용하여 배열의 첫 번째 요소를 삭제
// splice() 함수를 사용하여 배열 요소를 삭제
// filter() 함수를 사용하여 조건에 일치하는 새 배열을 생성

